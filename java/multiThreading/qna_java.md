1. What is the real difference between HashMap and ConcurrentHashMap and when should you never use one?
2. Why is String immutable in Java, and how does that impact performance?
3. What actually happens in memory when you create an object using new?
4. Difference between checked and unchecked exceptions and when to create your own?
5. How does JVM memory (Heap, Stack, Metaspace) really work?
6. What problem does Garbage Collection solve and when does it fail?
7. Why is composition preferred over inheritance?
8. What’s the difference between synchronized, volatile, and Atomic classes?
9. How does Java achieve platform independence?
10. When would you choose Stream API over a traditional loop and when not to?
